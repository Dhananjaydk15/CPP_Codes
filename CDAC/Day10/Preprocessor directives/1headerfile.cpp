#include <stdio.h>
#include "custom.h" // call custom header file :
int main()
{
    printf("\n\t* integer Veriable define in custom.h header file is %d.", ver1);
    // integer ver1 is defined in custom header file:
    printf("\n\t* float Veriable define in custom.h header file is %.2f.", ver2);
    // integer ver2 is defined in custom header file:
    return 0;
}